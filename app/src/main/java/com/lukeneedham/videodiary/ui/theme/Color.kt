package com.lukeneedham.videodiary.ui.theme

import androidx.compose.ui.graphics.Color

// Cinematic dark surfaces - let the video be the brightest thing on screen
val AppBackground = Color(0xFF0A0A0A)
val AppSurface = Color(0xFF1C1C1E)
val AppSurfaceVariant = Color(0xFF2C2C2E)

// Accent colors
val AccentRecord = Color(0xFFFF453A)
val AccentAccept = Color(0xFF32D74B)
val AccentHighlight = Color(0xFFFFD60A)

// Glass overlay colors - translucent panels that float over full-bleed video
val GlassFill = Color(0x4D000000)
val GlassFillStrong = Color(0x80000000)
val GlassBorder = Color(0x33FFFFFF)
